// function to make the players 
players = (name, mark) => {
    return {
        name: name,
        mark: mark
    }
};

// Obect that stores the game board as an array 
const gameBoard = {
    board: ["", "", "", "", "", "", "", "", ""]
};

